import logo from '../assets/NJOTIFY.png';

export const Navbar = () => {
    return(
        <nav>
            <div className={"left"}>
                <img className={"logo"} src={logo} alt={""}/>
            </div>
            <div className={"right"}>

            </div>
        </nav>
    )
}